package reflectutil

/*import (
	"fmt"
	"reflect"
	"testing"
)

func TestStructOriginalFieldValueTree(t *testing.T) {
	type TestStruct struct {
		FieldA string
		FieldB string
	}
	v := TestStruct{}
	tree, _ := StructOriginalFieldValueTree(v)
	fmt.Println(tree)
}

func TestMap(t *testing.T) {
	m := map[string]interface{}{
		"FieldA": 1,
		"FieldB": 2,
	}

	_type := reflect.TypeOf(m)
	value := reflect.ValueOf(m)
	fmt.Println(_type.Kind() == reflect.Map)
	fmt.Println(value.Kind() == reflect.Map)

}*/
